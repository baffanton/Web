enum DirectionTypes {
    row = 'row',
    column = 'column',
    default = 'row',
}

enum JustifyContentTypes {
    flexStart = 'flex-start',
    center = 'center',
    stretch = 'stretch',
    flexEnd = 'flex-end',
    spaceBetween = 'space-between',
    spaceAround = 'space-around',
    default = 'flex-start',
}

enum AlignItemsTypes {
    flexStart = 'flex-start',
    center = 'center',
    stretch = 'stretch',
    flexEnd = 'flex-end',
    spaceAround = 'space-around',
    spaceBetween = 'space-between',
    default = 'flex-start',
}

export { DirectionTypes, JustifyContentTypes, AlignItemsTypes };
